name: invoices_pro
description: تطبيق إدارة الفواتير والمنتجات - يعمل بدون إنترنت مع دعم كامل للغة العربية

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # قاعدة البيانات
  sqflite: ^2.3.0
  path: ^1.8.3
  
  # الواجهة والتأثيرات
  flutter_slidable: ^3.0.1
  
  # التاريخ والوقت
  intl: ^0.18.1
  
  # الملفات والمشاركة
  file_picker: ^5.5.0
  path_provider: ^2.1.1
  share_plus: ^7.1.0
  
  # PDF والطباعة
  pdf: ^3.11.0
  printing: ^5.12.0
  
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  # assets:
    # إضافة أي صور أو أيقونات إذا لزم الأمر
    
  # تم إزالة الخطوط المخصصة لحل مشكلة البناء للإنتاج
  # سيتم استخدام الخط الافتراضي للنظام الذي يدعم العربية